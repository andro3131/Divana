---
import { useTranslations, type Locale } from '../../i18n/ui';
import { sectionIds } from '../../i18n/utils';
import { YOUTUBE_VIDEOS, SPOTIFY_ARTIST_ID } from '../../lib/constants';
import SectionHeading from '../ui/SectionHeading.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);
const sectionId = sectionIds.music[locale];

// Show first 4 videos prominently, rest in smaller grid
const featuredVideos = YOUTUBE_VIDEOS.slice(0, 4);
const moreVideos = YOUTUBE_VIDEOS.slice(4);
---

<section id={sectionId} class="py-24 px-4 md:px-8 bg-dark-950">
  <div class="max-w-7xl mx-auto">
    <SectionHeading title={t('music.title')} />

    <!-- Featured YouTube Videos -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8" data-animate>
      {featuredVideos.map((videoId) => (
        <div class="aspect-video rounded-xl overflow-hidden bg-dark-800">
          <iframe
            src={`https://www.youtube.com/embed/${videoId}`}
            title="YouTube video"
            class="w-full h-full"
            loading="lazy"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
          ></iframe>
        </div>
      ))}
    </div>

    <!-- More Videos (smaller) -->
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-16" data-animate>
      {moreVideos.map((videoId) => (
        <div class="aspect-video rounded-lg overflow-hidden bg-dark-800">
          <iframe
            src={`https://www.youtube.com/embed/${videoId}`}
            title="YouTube video"
            class="w-full h-full"
            loading="lazy"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
          ></iframe>
        </div>
      ))}
    </div>

    <!-- Spotify Player -->
    <div class="max-w-3xl mx-auto" data-animate>
      <h3 class="text-lg text-dark-300 text-center mb-4 tracking-wider uppercase">{t('music.listenOn')}</h3>
      <div class="rounded-xl overflow-hidden">
        <iframe
          src={`https://open.spotify.com/embed/artist/${SPOTIFY_ARTIST_ID}?utm_source=generator&theme=0`}
          width="100%"
          height="352"
          frameborder="0"
          allowfullscreen
          allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
          loading="lazy"
          style="border-radius: 12px;"
        ></iframe>
      </div>
    </div>
  </div>
</section>
